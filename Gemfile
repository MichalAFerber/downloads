source "https://rubygems.org"

# Mirrors the toolchain GitHub Pages uses to build the site (Jekyll +
# jekyll-theme-cayman + supported plugins). Run `bundle install`, then
# `bundle exec jekyll serve` to preview locally.
gem "github-pages", group: :jekyll_plugins
